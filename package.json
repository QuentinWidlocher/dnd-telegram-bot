{
  "name": "dnd-telegram-bot-monorepo",
  "scripts": {
    "build": "pnpm -r --filter=./packages/* run build"
  },
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "typegram": "^3.10.0",
    "typescript": "^4.6.4"
  }
}